# Copy to .env — it is loaded automatically and is gitignored.

APIFLOW_DATABASE_URL=postgresql://apiflow:apiflow@localhost:5432/apiflow
APIFLOW_SOURCES=sources
APIFLOW_LOG_LEVEL=INFO

# Credentials referenced by source YAML via ${VAR}
GITHUB_TOKEN=
GITHUB_REPO=python/cpython
